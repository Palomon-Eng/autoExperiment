# autoExperiment
This repository contains the code, details, part lists, and instructions of the automatic experiment

## Instructions

The service will start on boot. If it hasn't turned on by itself, open the command terminal on ubuntu using ```ctrl``` + ```alt``` + ```T``` , and use the command ```bash sudo systemctl restart reactor-console.service ```
